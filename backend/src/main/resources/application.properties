spring.application.name=VIBELY

spring.application.name=VIBELY

spring.datasource.url=jdbc:postgresql://localhost:5432/Vively
spring.datasource.username=postgres   # or the user you created
spring.datasource.password=vibely

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# PostgreSQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

